from setuptools import setup, find_packages

setup(
    name="resume-intelligence-engine",
    version="1.0.0",
    description="Advanced AI Resume Intelligence Engine for analyzing professional resumes",
    author="Resume AI Team",
    packages=find_packages(),
    python_requires=">=3.8",
    install_requires=[
        # Core dependencies
        "PyPDF2>=3.0.0",
        "python-docx>=1.1.0",
        "spacy>=3.7.0",
        "nltk>=3.8.0",
        "pandas>=2.0.0",
        "numpy>=1.24.0",
        "scikit-learn>=1.3.0",
        "textblob>=0.17.1",
        "rich>=13.0.0",
        # Web interface
        "Flask>=3.0.0",
        "Werkzeug>=3.0.0",
    ],
    extras_require={
        "dev": ["pytest>=7.0.0", "black>=23.0.0", "mypy>=1.0.0"],
    },
    entry_points={
        "console_scripts": [
            "resume-analyze=cli.resume_cli:main",
            "resume-web=web_app:main",
        ],
    },
)
